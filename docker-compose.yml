services:
  db_anel:
    container_name: anel-container
    image: postgres:15
    environment:
      POSTGRES_DB: db_anel
      POSTGRES_USER: usuario
      POSTGRES_PASSWORD: senha
    volumes:
      - ./Documents/DB:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    restart: always